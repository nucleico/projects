import React, { Fragment } from 'react'

const Spinner = (props) => {
    return (
        <Fragment>
            <h3 style={{color: "blue", fontWeight: "bold"}}>LOADING...</h3>            
        </Fragment>
    )
}

export default Spinner
